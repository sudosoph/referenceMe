'use strict';

angular.module('referenceMeApp')
  .controller('CategoriesCtrl', function ($scope) {
  	$scope.links = hipchatLinks;

    // for (i=0, i++, i<=$scope.links.length) {
    // 	$scope.
    // }

  });

